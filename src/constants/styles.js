import { StyleSheet } from 'react-native'

export const MAIN_STYLES = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    paddingTop: 50
  },

  title: {
    fontSize: 40,
    fontWeight: "bold",
  }
}) 