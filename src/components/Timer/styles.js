import { StyleSheet } from 'react-native'

export const TIMER_STYLES = StyleSheet.create({
  timer: {
    width: "80%",
    textAlign: "center",
    fontSize: 80,
    fontWeight: "500",
    borderRadius: 10,
    padding: 50,
    margin: 10,
  },

  btnTimer: {
    width: "80%",
    borderWidth: 2,
    borderColor: "#555",
    borderRadius: 10,
    padding: 10,
    marginTop: 5
  },

  btnTimerText: {
    fontSize: 27,
    textAlign: "center",
  }
})